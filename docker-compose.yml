version: '3.2'

services:
  transactions-api:
    image: transactions-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
  transactions-api-test:
    image: transactions-api-test
    build:
      context: .
      dockerfile: Dockerfile
    links:
     - transactions-api